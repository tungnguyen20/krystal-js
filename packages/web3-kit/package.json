{
    "name": "@krystal-js/web3-kit",
    "version": "0.0.3",
    "description": "SDK for web bridge to offer unified method signatures for Android/iOS.",
    "main": "dist/index.js",
    "license": "MIT",
    "scripts": {
        "build": "tsc",
        "bundle": "webpack --mode production",
        "prepublish": "yarn run clean && yarn run build && yarn run bundle",
        "clean": "rimraf dist && rimraf bundle"
    },
    "keywords": [
        "krystal",
        "web",
        "browser"
    ],
    "files": [
        "dist/**/*",
        "bundle/**/*"
    ],
    "dependencies": {
        "@krystal-js/common": "workspace:*"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "ts-loader": "^9.2.2",
        "typescript": "^4.3.2",
        "webpack": "^5.38.1",
        "webpack-cli": "^4.7.0"
    }
}
